[datasource]
  provider = "mysql"
  host = "10.0.0.3"
  port = 3306
  database = "vault_db"
  credential_mode = "vault-secret"
  vault_path = "secret/data/myapp/db"
  vault_username_field = "db_user"
  vault_password_field = "db_pass"
